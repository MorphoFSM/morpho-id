document.addEventListener('DOMContentLoaded', () => {
    console.log('BioMorph: Halaman Utama (Index) berjaya dimuatkan.');

    // Nota: Halaman Index tak perlukan fungsi Javascript yang berat
    // sebab tiada kotak carian (search) di sini.
    // Semua efek visual bergantung sepenuhnya pada CSS.
});
